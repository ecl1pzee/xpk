alright, yeah no big plans, but another one is this:

alongside /opt/xpk/systree/systree-N, we are gonna store a binary list of all packages (binary being actually useful here since users would likely have thousands of packages)
and the merged systree hash.